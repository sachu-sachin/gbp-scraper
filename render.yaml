services:
  - type: web
    name: gbp-scraper
    env: node
    plan: free
    buildCommand: "npm install"
    startCommand: "node server.js"
    envVars:
      - key: PORT
        value: 3000
      - key: NODE_ENV
        value: production
